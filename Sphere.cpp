#include "Sphere.h"

Sphere::Sphere(const vector3& pos, double radius) : pos(pos), radius(radius) 
{}

double Sphere::intersect(const ray_t& ray, vector3& hit, vector3& normal) {
	//TODO
	return 0;
}
